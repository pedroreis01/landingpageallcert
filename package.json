{
  "name": "landingpageallcert-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd astro-site && npm install && npm run build && cd .. && rm -rf dist && cp -r astro-site/dist dist"
  }
}
